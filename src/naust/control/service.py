"""Project 0 Control stub; lifecycle orchestration arrives in Project 4."""

from naust.control.config import ControlConfig


def run_control(config: ControlConfig) -> None:
    """Accept validated configuration and exit cleanly."""
